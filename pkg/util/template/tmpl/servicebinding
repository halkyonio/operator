apiVersion: servicecatalog.k8s.io/v1beta1
kind: ServiceBinding
metadata:
  name: {{.Name}}
  labels:
    app: {{.Name}}
    name: {{.Name}}
spec:
  instanceRef:
    name: {{.Name}}
  secretName: {{.Name}}